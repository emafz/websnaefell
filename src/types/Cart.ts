export interface CartItem {
  productId: string;
  productSlug: string;
  variantId: string;
  variantSlug: string;
  sku: string;
  name: string;
  color: string;
  price: number;
  quantity: number;
  image: string;
}
