import { Link } from "react-router-dom";
import { formatCurrency } from "../../utils/currency";
import "./CompraExitosa.css";
export default function CompraExitosa(){let order:{id:string;total:number}|null=null;try{const raw=sessionStorage.getItem("snaefell-last-order");order=raw?JSON.parse(raw):null}catch{order=null}return <section className="success-page"><div className="container success-card"><span className="success-icon">✓</span><span className="eyebrow">Pedido registrado</span><h1>Gracias por elegir Snaefell.</h1><p>Este starter está trabajando en modo demostración. Cuando conectes el backend, esta pantalla podrá mostrar el estado real del pago y del envío.</p>{order&&<div className="success-data"><span>Pedido <strong>{order.id}</strong></span><span>Total <strong>{formatCurrency(order.total)}</strong></span></div>}<div className="success-actions"><Link className="button button--dark" to="/tienda">Volver a la tienda</Link><Link className="button button--ghost" to="/">Ir al inicio</Link></div></div></section>}
